# 不動産登記書類整合性審査

住宅ローン関連の登記書類（抵当権設定）について、複数のスキャン書類間および書類と案件情報との整合性を審査するコンテキスト。旧 AgentCore harness が「事前抽出済み OCR データ＋案件情報」の JSON 入力で行っていた審査を、本プロジェクトではスキャン PDF/画像の多模态読取に置き換えて再現する。

## 入力データ

**案件情報** (Case Information):
審査の基準となる、システム側で抽出・入力された案件データ。顧客氏名・借入情報・物件情報などから成るネストされた名値対（LLM が理解できる自然言語の値）。
_Avoid_: 書類OCRデータ（別物・本プロジェクトでは廃止）、入力データ、formData、申請データ

**スキャン書類** (Scanned Document):
審査対象となる、PDF または画像形式でアップロードされた書類のスキャン。
_Avoid_: ファイル、ドキュメント、添付

**文書タイプ** (Document Type):
スキャン書類の業務上の種別。本シナリオでは 抵当権設定契約証書 / 登記完了証 / 登記情報識別通知 / 登記簿謄本 の4種。1 書類タイプにつき原則1通のスキャンを対応させる。
_Avoid_: fileType（pdf/image は技術的キャリアであり業務種別ではない）、書類フォーマット（曖昧）

## 審査

**比較ルール** (Comparison Rule):
審査項目の元になる判定基準。比較元（書類の節・フィールド、または案件情報のフィールド、または固定値）と比較先、および比較方法（一致・存在・個数一致・日付の先後・レコードのペアリング等）から成る。本シナリオでは17件。
_Avoid_: 検証ルール、チェック条件

**審査項目** (Review Item):
1つの比較ルールに対応する CheckList の葉項目。スキャン書類と案件情報を照合し、合格 / 不合格 / 判定不能 を判定する。
_Avoid_: チェック項目、ルール（比較ルールは審査項目の**元**になる基準を指す）

**判定不能** (Undeterminable):
比較対象のいずれかが欠損・空白・null のため、合格とも不合格とも判定できない第3の状態。
_Avoid_: 要確認（UI 表示語としては併用するが、状態の正名は「判定不能」）、pending、unknown
